{{ form_start(form) }}
<div class="row">
	<div class="col-md-6 mb-3">
		{{ form_row(form.cnpj) }}
	</div>
	<div class="col-md-6 mb-3">
		{{ form_row(form.corporate_name) }}
	</div>
	<div class="col-md-6 mb-3">
		{{ form_row(form.dt_foundation) }}
	</div>
</div>

<button type="submit" class="btn btn-primary mt-3">{{ button_label|default('Salvar') }}</button>
{{ form_end(form) }}
